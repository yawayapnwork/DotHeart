<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-build-only (see ../AndroidManifest.xml). Scopes cleartext HTTP to
  local development hosts only, rather than permitting it everywhere -
  if DOTHEART_BASE_URL is ever accidentally pointed at a non-HTTPS
  production-like host, this config still blocks it even in a debug build.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <!-- Android emulator's alias for the host machine's loopback address. -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
